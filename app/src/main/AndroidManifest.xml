<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GroupProject"
        tools:targetApi="31">

        <!-- Existing SearchAndFilterActivity -->
        <activity
            android:name=".SearchAndFilterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- New SocialSharingActivity -->
        <activity
            android:name=".SocialSharingActivity"
            android:exported="true">
            <!-- No intent-filter needed here unless this is also a MAIN activity -->
        </activity>

        <!-- Add other activities as needed -->

    </application>

</manifest>
